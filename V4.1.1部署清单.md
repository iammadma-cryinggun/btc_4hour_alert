# V4.1.1 部署检查清单

## 文件检查 ✅

### 必需文件
- [ ] `BTC4小时策略预警v4.1.1_双信号版.py` - 主程序
- [ ] `dxy_data.csv` - DXY数据
- [ ] `requirements_v411.txt` - Python依赖

### 配置文件（自动生成）
- [ ] `position_tracker_aligned_status.json` - 持仓状态
- [ ] `battle_aligned_status.json` - 战备状态
- [ ] `basic_physics_signals.json` - 基础信号记录
- [ ] `trade_history.json` - 交易历史

### 文档文件
- [ ] `V4.1.1云端部署指南.md`
- [ ] `V4.1.1更新说明.md`

## 环境检查 ✅

### Python环境
```bash
python3 --version  # 应该 >= 3.8
```
- [ ] Python版本正确

### 依赖安装
```bash
pip3 list | grep -E "numpy|pandas|scipy|requests|schedule"
```
- [ ] numpy >= 1.21.0
- [ ] pandas >= 1.3.0
- [ ] scipy >= 1.7.0
- [ ] requests >= 2.26.0
- [ ] schedule >= 1.1.0

## 网络检查 ✅

### API连接测试
```python
# 测试Binance API
import requests
resp = requests.get("https://api.binance.com/api/v3/ping")
print("Binance:", "OK" if resp.status_code == 200 else "FAIL")

# 测试Coinalyze API
resp = requests.get("https://api.coinalyze.net/v1")
print("Coinalyze:", "OK" if resp.status_code == 200 else "FAIL")

# 测试Telegram API
TOKEN = "你的token"
resp = requests.get(f"https://api.telegram.org/bot{TOKEN}/getMe")
print("Telegram:", "OK" if resp.status_code == 200 else "FAIL")
```
- [ ] Binance API可访问
- [ ] Coinalyze API可访问（可能需要代理）
- [ ] Telegram API可访问

## 配置检查 ✅

### Telegram配置（第180-181行）
```python
self.telegram_token = "8189663571:AAEvIUEBTfF_MfyKc7rWq5gQvgi4gAxZJrA"
self.telegram_chat_id = "838429342"
```
- [ ] Token已配置
- [ ] Chat ID已配置
- [ ] 测试消息已发送成功

### 代理配置（第173-177行，可选）
```python
self.proxy_enabled = True   # 如果需要代理
self.proxy_host = "127.0.0.1"
self.proxy_port = "15236"
```
- [ ] 如果需要代理，已配置
- [ ] 代理连接测试成功

## 功能测试 ✅

### 1. 启动测试
```bash
python3 BTC4小时策略预警v4.1.1_双信号版.py
```
预期输出：
```
================================================================================
物理奇点预警系统 V4.1.1 双信号版
================================================================================
...
✅ 基础物理信号追踪器: 已初始化
✅ Smart Ape风险管理器: 已初始化
...
```
- [ ] 程序正常启动
- [ ] 无错误信息

### 2. 基础信号记录测试
等待下一个4H收盘点，或手动触发：
```bash
# 查看日志
tail -f physics_alert_v3_1_complete.log | grep "\[基础信号\]"
```
预期输出：
```
[基础信号] 开始计算基础物理信号...
[基础信号] 已记录: BEARISH_SINGULARITY | 张力=0.452 | ...
```
- [ ] 基础信号正常计算
- [ ] JSON文件已更新
- [ ] Telegram通知已发送（如果有SINGULARITY信号）

### 3. V4.1交易系统测试
验证V4.1功能正常：
- [ ] 4H信号检测正常
- [ ] EMA21入场正常
- [ ] 仓位管理正常
- [ ] 止盈止损正常

## 运行状态检查 ✅

### 进程状态
```bash
ps aux | grep BTC4小时策略预警
```
- [ ] 进程正在运行

### 日志文件
```bash
tail -20 physics_alert_v3_1_complete.log
```
- [ ] 日志正常写入
- [ ] 无严重错误

### 状态文件
```bash
ls -lh position_tracker_aligned_status.json
ls -lh battle_aligned_status.json
ls -lh basic_physics_signals.json
```
- [ ] 状态文件存在
- [ ] 文件大小合理（不是0字节）

## 备份检查 ✅

### 数据备份
- [ ] 创建备份目录
- [ ] 设置定时备份（crontab或手动）
- [ ] 验证备份脚本正常工作

### 备份测试
```bash
# 运行一次备份
bash backup.sh

# 检查备份文件
ls -lh ~/btc-physics-backup/
```
- [ ] 备份文件生成成功

## 监控设置 ✅

### 日志监控
- [ ] 设置日志轮转（logrotate）
- [ ] 配置错误日志告警

### 进程监控
```bash
# systemd方式
sudo systemctl status btc-physics-v411.service

# 或使用监控脚本
watch -n 60 'ps aux | grep BTC4小时策略预警'
```

### Telegram通知
- [ ] 测试Telegram通知正常
- [ ] 验证命令响应正常
  - `/status` - 查看持仓
  - `/help` - 帮助信息
  - `/close` - 平仓测试

## 性能检查 ✅

### CPU使用率
```bash
top -p $(pgrep -f BTC4小时策略预警)
```
- [ ] CPU使用率正常（<5%）

### 内存使用
```bash
ps aux | grep BTC4小时策略预警 | awk '{print $6}'
```
- [ ] 内存使用正常（<200MB）

### 磁盘空间
```bash
df -h
```
- [ ] 磁盘空间充足（>1GB）

## 部署完成 ✅

### 最终确认
- [ ] 所有文件已上传
- [ ] 所有依赖已安装
- [ ] 所有配置已完成
- [ ] 所有测试已通过
- [ ] 备份策略已设置
- [ ] 监控已配置

### 启动生产环境
```bash
# 使用systemd
sudo systemctl start btc-physics-v411.service

# 或使用nohup
nohup python3 BTC4小时策略预警v4.1.1_双信号版.py > output.log 2>&1 &
```

### 验证生产运行
- [ ] 进程持续运行（>10分钟）
- [ ] 无异常退出
- [ ] 日志正常输出
- [ ] Telegram通知正常

---

## 故障排查

### 问题：程序启动失败
**检查**：
```bash
python3 BTC4小时策略预警v4.1.1_双信号版.py
# 查看错误信息
```

### 问题：基础信号不记录
**检查**：
```bash
# 查看日志
grep "基础信号" physics_alert_v3_1_complete.log

# 检查JSON文件权限
ls -l basic_physics_signals.json
```

### 问题：Telegram收不到消息
**检查**：
```python
# 手动测试Telegram
import requests
TOKEN = "你的token"
CHAT_ID = "你的chat_id"
resp = requests.post(f"https://api.telegram.org/bot{TOKEN}/sendMessage",
                    json={"chat_id": CHAT_ID, "text": "测试消息"})
print(resp.json())
```

### 问题：API连接失败
**检查**：
1. 代理配置是否正确
2. 防火墙是否阻止
3. API密钥是否有效

---

**部署检查完成！祝交易顺利！🚀**
